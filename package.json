{
  "name": "fit4110-lab05-nhom2-aivision",
  "version": "1.0.0",
  "description": "FIT4110 Lab 05 - Docker Compose and Newman verification",
  "private": true,
  "scripts": {
    "test:local": "newman run postman/collections/FIT4110_lab03_ai_vision.postman_collection.json --env-var baseUrl=http://localhost:8000 --env-var cameraStreamMockUrl=http://localhost:8000 -r cli,html,junit --reporter-html-export reports/newman-lab05.html --reporter-junit-export reports/newman-lab05.xml"
  },
  "devDependencies": {
    "newman": "^6.2.1"
  },
  "dependencies": {
    "newman-reporter-html": "^1.0.5"
  }
}